name: lsp_domain
description: LSP domain layer - platform-agnostic LSP abstractions
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  freezed_annotation: ^2.4.4
  dartz: ^0.10.1
  meta: ^1.15.0

  # Editor core domain
  editor_core:
    path: ../editor_core

dev_dependencies:
  freezed: ^2.5.7
  build_runner: ^2.4.13
  test: ^1.25.8
