name: editor_core
description: Core domain layer for multi-file code editor - entities, value objects, and ports
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: ^3.1.0
  json_serializable: ^6.8.0
  build_runner: ^2.4.12
  flutter_lints: ^5.0.0
