name: lsp_application
description: LSP Application Layer - Use Cases and Application Services
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  # Domain layers
  editor_core:
    path: ../editor_core
  lsp_domain:
    path: ../lsp_domain

  # Utilities
  dartz: ^0.10.1
  freezed_annotation: ^2.4.4
  meta: ^1.15.0
  rxdart: ^0.28.0

dev_dependencies:
  freezed: ^2.5.7
  build_runner: ^2.4.13
  test: ^1.25.8
  mocktail: ^1.0.4
