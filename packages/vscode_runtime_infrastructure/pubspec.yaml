name: vscode_runtime_infrastructure
description: VS Code Runtime Management - Infrastructure Layer
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  # Domain layer
  vscode_runtime_core:
    path: ../vscode_runtime_core

  # Functional programming
  dartz: ^0.10.1

  # HTTP
  dio: ^5.4.0

  # File system
  path: ^1.8.3
  path_provider: ^2.1.1

  # Archive extraction
  archive: ^3.4.9

  # Crypto for hashing
  crypto: ^3.0.3

  # Platform detection
  platform: ^3.1.3

  # JSON
  json_annotation: ^4.8.1

  # Streams
  rxdart: ^0.27.7

dev_dependencies:
  build_runner: ^2.4.6
  json_serializable: ^6.7.1
  test: ^1.24.0
  mocktail: ^1.0.1
  mockito: ^5.4.4
