name: js_ts_ide_enhancements
description: JavaScript/TypeScript-specific IDE enhancements - npm/yarn commands, Node.js tools, and JS/TS-specific workflows
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  # Domain layers
  editor_core:
    path: ../editor_core
  lsp_domain:
    path: ../lsp_domain

  # Application layer
  lsp_application:
    path: ../lsp_application

  # Functional programming
  dartz: ^0.10.1

  # Utilities
  path: ^1.9.0
  process: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
