name: lsp_infrastructure
description: LSP Infrastructure Layer - WebSocket client and protocol adapters
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  # Domain layers
  editor_core:
    path: ../editor_core
  lsp_domain:
    path: ../lsp_domain

  # WebSocket
  web_socket_channel: ^3.0.0

  # Utilities
  dartz: ^0.10.1
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  uuid: ^4.4.0
  meta: ^1.15.0
  injectable: ^2.5.0
  get_it: ^8.0.2

dev_dependencies:
  freezed: ^2.5.7
  json_serializable: ^6.8.0
  build_runner: ^2.4.13
  test: ^1.25.8
  mocktail: ^1.0.4
  injectable_generator: ^2.6.2
